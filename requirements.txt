# core detection + geometry
numpy
opencv-contrib-python
ultralytics            # FastSAM ring detection (pulls torch, torchvision)

# GUI (scripts/gui.py) - Pillow shows the annotated image in the window.
# Tkinter ships with standard Python on Windows/macOS; on Linux install the
# OS package if missing (e.g. sudo apt install python3-tk).
pillow

# reading the robot_xy .xls (optional; only for robot_map.py if using .xls)
pandas
xlrd
